# IDEs
.idea/
*.iml
.vscode/

# OS
.DS_Store
Thumbs.db

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment
.env
.env.*

# Local plugin-dev MCP server config. A .mcp.json at the plugin root is
# distributed to every app that installs the plugin, so we keep this file
# local-only and let each developer maintain their own.
.mcp.json

# skill-creator eval workspace artifacts (generated, not committed)
skills/*-workspace/
.eval-workspace/
